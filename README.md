# Лабораторная работа №10: Разработка React-приложения
## Проект: Todo Pro — Личный планировщик задач

**Todo Pro** — это современный таск-менеджер, ориентированный на продуктивность. Приложение позволяет структурировать ежедневные дела, фильтровать их по дедлайнам и отслеживать прогресс выполнения в реальном времени.

## 🛠 Функционал
* **Управление задачами:** Создание, удаление и отметка о выполнении.
* **Умные фильтры:** Сортировка задач по временным отрезкам (Все / Сегодня / Завтра).
* **Мини-статистика:** Динамический счетчик выполненных и оставшихся задач.
* **Persistence (Локальное сохранение):** Все данные сохраняются в `localStorage` браузера и не пропадают после перезагрузки страницы.
* **Адаптивный интерфейс:** Корректное отображение на мобильных устройствах и десктопе.

---

## 🌐 Используемое API и примеры запросов
В данном проекте используется **Web Storage API (localStorage)** для имитации работы базы данных на стороне клиента.

### Пример записи данных:
```javascript
// Сохранение массива задач в локальное хранилище
useEffect(() => {
  localStorage.setItem('todo_tasks', JSON.stringify(tasks));
}, [tasks]);
```
Какие части сгенерировал ИИ:
Базовая верстка: Генерация JSX-структуры для карточек задач и контейнеров.

CSS-стили: Написание конфигурации Flexbox для корректного выравнивания элементов списка.

Логика фильтрации: Шаблон функции, которая сравнивает текущую дату с датой задачи (new Date().toISOString()).

Какие части написаны вручную:
Архитектура компонентов: Разделение приложения на App.js, TaskItem.js, Filters.js и Stats.js.

Управление состоянием: Реализация хуков useState и useEffect для синхронизации с localStorage.

Валидация ввода: Запрет на создание пустых задач или задач с прошедшей датой.

Ошибки и решения
1. Ошибка синхронизации LocalStorage
Проблема: При первой загрузке приложение падало с ошибкой, так как JSON.parse пытался обработать null.

Решение: Добавлена проверка (ленивая инициализация в useState), которая возвращает пустой массив, если ключ в хранилище отсутствует.

2. Ошибка "Hydration" при фильтрации дат
Проблема: Разница в часовых поясах приводила к тому, что фильтр "Сегодня" иногда пропускал задачи, созданные поздним вечером.

Решение: Использовал метод .setHours(0, 0, 0, 0) для приведения всех дат к единому формату начала дня перед сравнением.

3. Удаление задач
Проблема: После удаления задачи статистика не обновлялась мгновенно.

Решение: Переписал функцию удаления через setTasks(prev => prev.filter(...)), чтобы React гарантированно видел изменение ссылки на массив и инициировал ререндер.
